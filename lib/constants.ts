export const IMAGES = {
  NAV_LOGO: './nav_logo.png',
};

export enum API_URL {
  VERSION = '/api/v1',
  LOGIN = '/auth/login',
  SEED = '/seed',
  ACCESS_TOKEN = 'access_token',
  LOCALHOST = 'http://localhost:3000',
  API = 'http://localhost:3001',
}

